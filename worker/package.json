{
  "name": "facebook-scraper-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Facebook Scraper",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "pocketbase": "^0.28.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
